# ReviewPulse v3.0.0 - Aspect-Based Sentiment Analysis

Status: **release candidate**, with implementation, evaluation, independent validation and report
content all closed. What remains is the release ritual: the final report export, the packaged
archives built from it, the `v3.0.0` tag and submission.  
Track: DLE602 Assessment 3  
Last updated: 2026-08-15 AEST

## Summary

ReviewPulse v3.0.0 is the DLE602 evolution of the product delivered for ISY503. Versions 1.x and 2.x classify the overall sentiment of an Amazon review. Version 3.0 adds an isolated aspect-based sentiment analysis (ABSA) workflow that predicts `negative`, `neutral` or `positive` for each restaurant aspect supplied by the user.

The legacy v2.3.0 workflow remains available and its binary API and artifacts are not reused by v3. The new implementation lives under `src/absa/`, with separate data contracts, model artifacts, evaluation outputs and application entry points.

This document is the consolidated delivery record for v3. Detailed experiment contracts remain in `docs/dle602-a3/`, and GitHub issue [#72](https://github.com/lfariabr/review-pulse/issues/72) remains the implementation tracker.

## Delivery Status

| Area | Delivered in the candidate | Remaining release gate |
|---|---|---|
| Data and task contract | Restaurants parser, offset audit, fixed labels, grouped splits and mixed-polarity subset | None for core scope |
| Four-model ladder | TF-IDF, target-agnostic LSTM, ATAE-LSTM and DistilBERT sentence pair | None; verified together in #84 |
| Training controls | Seeded runs, regularisation, early stopping, best-checkpoint restoration and run metadata | None for the four-model core |
| Common evaluation | Reproducible runner, shared predictions, metrics, efficiency evidence, confusion matrices and error candidates | None; #84 evidence reviewed |
| Application | Separate v2/v3 pages, manual multi-aspect input, six-model selection, sample generation and controlled missing-artifact errors | None; Juan Martinez executed 12 deployed-Streamlit cases in PR #120, hardened in PR #121. Three acceptance failures and two unreproducible stale-state observations ship as known documented findings and never as release blockers |
| Token evidence | Exact-offset ATAE heatmap, DistilBERT gradient × input attribution and explicit unsupported baseline states | None; #85 reviewed and merged |
| Optional six-model track | Matched GRU/TextCNN candidates plus explicit verified training, evaluation, app and supplemental-output mode under #94–#96 | None; verified from commit `cef08fa` |
| Independent reproduction | Victor Dorantes reproduced the stored metrics from all six confusion matrices, retrained and evaluated the four canonical models on CUDA, then verified the shipped artifacts without retraining | None; all seven Appendix F checks record observed command output and a Pass. PR #123, merge commit `8787a73`, recorded in `docs/dle602-a3/validation-victor.md` |
| Academic package | Report content complete and frozen at `lfariabr/masters-swe-ai@6eaad14`, source `report/DLE602_A3_Report_v4.md`, including Appendices A-H, the contribution log and the integrity statement | Final PDF and DOCX export only |
| Release package | Release notes, v3-first README, DLE602 checklist and the `SUBMISSION_README.md` front door | Tag `v3.0.0`, publish the digests with the release and submit under issue #89. Both archives are built and verified; the report uploads separately, so nothing here waits on the export |

## 1. Data and Task Contract

The v3 core dataset is the SemEval-2014 Task 4 **Restaurants** domain. Laptops, automatic aspect extraction and topic modelling are outside the core release.

- Training and evaluation use the SemEval gold aspect terms.
- The application asks the user to enter one or more aspects manually.
- Labels have the fixed order `negative`, `neutral`, `positive`.
- The original SemEval `conflict` label is counted during audit and excluded from three-class modelling.
- The **mixed-polarity multi-aspect subset** contains sentences with at least two retained aspects assigned different polarities.
- Development splits are grouped by sentence so instances from the same sentence cannot leak across partitions.
- Raw sentence text and character offsets remain authoritative; v2 text cleaning is not applied to ABSA alignment.

Acquisition, licensing constraints and checksum recording are documented in `docs/dle602-a3/semeval-restaurants.md`.

## 2. Four-Model Comparison Ladder

All four models use the same retained examples, label mapping and official Restaurants test partition.

| Model | Input contract | Purpose |
|---|---|---|
| TF-IDF + Logistic Regression | Review only | Lightweight classical baseline |
| Target-agnostic LSTM | Review only | Controlled neural baseline without aspect conditioning |
| ATAE-LSTM | Review and aspect | Lightweight aspect-conditioned model with attention weights |
| DistilBERT sentence pair | Review and aspect | Pretrained contextual aspect-conditioned model |

The review-only models intentionally do not receive the target aspect. Their behaviour on mixed-polarity sentences is part of the comparison, never an accidental input mismatch. No v2 binary checkpoint or metric is accepted as v3 evidence.

The optional target-agnostic GRU is trained independently as a controlled recurrent ablation against the LSTM. It is review-only, uses the same dimensions and training budget, reports token evidence as unsupported and does not alter the canonical four-model outputs. Its protocol is documented in `docs/dle602-a3/target-gru.md`.

The verified six-model run reached 0.6750 full-test accuracy and 0.4603 macro-F1 for GRU, versus 0.6687 and 0.4326 for LSTM. On the mixed-polarity subset GRU was slightly weaker (0.4079 accuracy, 0.3156 macro-F1 versus 0.4167 and 0.3264). It used 10.31% fewer parameters and produced a 9.99% smaller artifact. Timing from the shared CPU run is retained in the supplemental result.

The optional review-only TextCNN is a low-compute, non-recurrent sentence-level baseline and not a reproduction of the cited Twitter CNN. A bounded three-configuration gate selected widths `(3,4,5)` with 100 filters per width by development macro-F1 without evaluating the official test. The verified six-model run reached 0.6893 full-test accuracy and 0.4498 macro-F1; on the mixed-polarity subset it reached 0.4167 accuracy and 0.3106 macro-F1. It used 456,203 parameters, produced a 1.81 MB artifact and trained in 25.13 seconds on CPU. Its stronger accuracy but zero mixed-polarity neutral recall is retained as evidence that a different review-only encoder does not solve the missing-aspect limitation. The predictor repeats review-level outputs per supplied aspect and explicitly marks token evidence as unsupported. Its protocol is documented in `docs/dle602-a3/text-cnn.md`.

The complete same-commit table, per-class evidence, provenance, error counts and interpretation are recorded in `docs/dle602-a3/six-model-results.md`.

## 3. Reproducible Training

Issue [#91](https://github.com/lfariabr/review-pulse/issues/91) aligned the implementation with the submitted A2 methodology:

- Python, NumPy and PyTorch are seeded before split, model and data-loader creation.
- CUDA, Apple MPS and CPU device selection are supported where applicable.
- Neural optimisers use recorded weight decay.
- Development macro-F1 selects checkpoints; the best state is restored before final metrics are produced.
- Early stopping, complete epoch history, best epoch and overfitting diagnostics are persisted.
- Training duration, configuration, dataset checksums, seed and source commit are stored with the artifacts.
- Loss functions are instantiated outside batch loops.

The exact defaults and the fixed-seed versus multi-seed decision rule are defined in `docs/dle602-a3/training-protocol.md`.

## 4. Common Evaluation and Error Analysis

Issue [#84](https://github.com/lfariabr/review-pulse/issues/84) provides one command that loads the four verified model families and evaluates them over one shared official-test prediction set.

The evaluation records:

- accuracy, macro-F1, per-class metrics and confusion matrices;
- full-test and mixed-polarity multi-aspect results;
- training time, artifact size, cold start, first-prediction time, warm latency and throughput;
- representative conditioned-model wins, review-only wins, disagreements and common errors;
- source commit, seed, device, dataset checksums and the SHA-256 digest of the shared predictions.

Generated files below `outputs/absa/evaluation/` are:

| File | Release evidence |
|---|---|
| `results.json` | Metrics, environment, provenance and efficiency measurements |
| `predictions.csv` | Shared gold labels and four-model predictions |
| `comparison.md` | Report-ready comparison table |
| `confusion_matrices.png` | Four official-test confusion matrices |
| `error_analysis.json` | Ordered error categories and candidate examples |

The default runner rejects older artifacts that do not contain the reproducibility metadata introduced by issue #91. The diagnostic override for legacy local files must not be used for report claims. Issue #84 regenerated all four artifacts from commit `bf36c3b`, verified 1,120 shared official-test predictions and reviewed the resulting metrics, efficiency records, confusion matrices and bidirectional error cases.

## 5. Application Experience

The Streamlit landing page presents two deliberately separate product phases:

- **v2.3.0 / ISY503:** binary sentiment for one complete Amazon review.
- **v3.0.0 / DLE602:** three-class sentiment for one or more manually entered restaurant aspects.

The v3 page supports comma-separated aspects, ordered per-aspect results, model selection and sample generation for repeatable demonstrations. TF-IDF and the target-agnostic LSTM receive only the review; ATAE-LSTM and DistilBERT receive the review-and-aspect pair. Missing data, dependencies or model artifacts produce controlled application errors and never fall back silently to a v2 model.

The selector also exposes the exploratory review-only GRU and TextCNN. Both repeat the review-level result for each supplied aspect and explicitly report that aspect-specific token evidence is unsupported.

## 6. Indicative Token Evidence

Interpretability claims are intentionally limited.

- ATAE-LSTM displays its learned aspect-conditioned attention over exact visible review spans.
- DistilBERT displays gradient × input attribution for the predicted class, with review wordpieces aggregated by fast-tokenizer offsets.
- TF-IDF, the target-agnostic LSTM, the optional target-agnostic GRU and the optional review-only TextCNN explicitly report that aspect-specific token evidence is unsupported.
- Heatmaps preserve visible case, punctuation and whitespace, and their payloads name the method and limitation.
- Any displayed scores are **indicative token-level evidence**, not model reasoning or a causal explanation.

The method, alignment contract, representative `food`/`service` export and limitations are documented in `docs/dle602-a3/token-evidence.md`.

## 7. Reliability and Safety

The candidate includes focused protection for the new pipeline:

- external SemEval XML is parsed with `defusedxml`;
- parser tests fail when available source XML unexpectedly produces no examples;
- split tests assert deterministic sentence-level separation;
- checkpoint tests protect label mappings, configuration and clean loading;
- legacy v2 and isolated v3 workflows have non-regression coverage;
- the merged PR #100 baseline recorded **266 passed, 8 skipped**;
- the initial #89 package candidate recorded **271 passed, 8 skipped**, including five deterministic-package tests;
- the final pre-release development suite records **363 passed, 3 skipped** after #115 moved parser aggregation onto synthetic four-domain fixtures.

The three remaining skips are the accuracy, full-evaluation and one-epoch
training checks whose purpose genuinely depends on the optional, gitignored
legacy Amazon dataset.

Skip counts are a property of the environment, and every skip is an intentional absence of licensed
data or Git metadata. None is a failure.

| Environment | Passed | Skipped | Why the skips differ |
|---|---:|---:|---|
| Development machine | 363 | 3 | Legacy Amazon `.review` corpus is not redistributed |
| Clean GitHub clone | 357 | 9 | Six sample-provenance checks additionally need the non-redistributed `predictions.csv` |
| Extracted lightweight archive | 355 | 11 | Two package-builder checks additionally need Git metadata, which a ZIP does not carry |

## Canonical Commands

From the repository root in the recorded Python environment:

```bash
# Verify the isolated v3 path.
.venv/bin/python -m pytest tests/absa -q
.venv/bin/python scripts/smoke_absa.py
.venv/bin/python scripts/export_absa_evidence.py

# Train and smoke the optional fifth-model candidate without widening core outputs.
.venv/bin/python -m src.absa.training.runner --models target_gru --device cpu
.venv/bin/python scripts/smoke_target_gru.py

# Select, train and smoke the optional sixth-model candidate.
.venv/bin/python scripts/select_text_cnn_config.py
.venv/bin/python scripts/train_selected_text_cnn.py --device cpu
.venv/bin/python scripts/smoke_text_cnn.py

# Regenerate all six from one commit and write the separate supplement.
.venv/bin/python scripts/train_selected_text_cnn.py --all-six --device auto
.venv/bin/python -m src.absa.evaluation.runner \
  --models tfidf target_lstm target_gru text_cnn atae_lstm distilbert \
  --device auto

# Regenerate all four verified artifacts.
.venv/bin/python -m src.absa.training.runner --device auto

# Produce the common comparison package.
.venv/bin/python -m src.absa.evaluation.runner --device auto

# Run the full product test suite and application.
.venv/bin/python -m pytest tests/ -q
.venv/bin/streamlit run app.py
```

SemEval source files and generated evaluation outputs remain local and gitignored. The six verified inference checkpoints are versioned through Git LFS, including the DistilBERT `model.safetensors`, so Streamlit Community Cloud receives them during checkout. The release package preserves their checksums and reproducibility metadata without redistributing restricted data.

## Submission Packaging

Two archives are built from one frozen commit by `scripts/build_a3_package.py`, which works from an
explicit allowlist, refuses a dirty working tree, rejects unresolved Git LFS pointers and writes a
`PACKAGE_MANIFEST.json` recording the byte size and SHA-256 of every entry. Entry timestamps are
fixed to the source commit time and paths are sorted, so identical inputs produce identical ZIP
bytes. `docs/dle602-a3/SUBMISSION_README.md` is the front door for a marker opening the upload.

| Archive | Build mode | Size | v3 models |
|---|---|---:|---:|
| Lightweight | `--artifact-mode lightweight` | approx. 52 MB | 5 of 6 |
| Complete | `--artifact-mode all` | approx. 288 MB | 6 of 6 |

The single v3 DistilBERT directory is the whole difference between them, at roughly 256 MB.
Selecting that model in the lightweight archive reports it unavailable and returns no prediction;
a missing artifact is always reported and never silently substituted. `scripts/smoke_absa.py`
exercises it and is therefore deliberately unusable there, with `scripts/smoke_target_gru.py` and
`scripts/smoke_text_cnn.py` as the equivalent clean-load checks.

Neither archive carries the SemEval XML, any row-level export derived from it, caches, credentials
or virtual environments. `data/semeval2014/` ships empty, and the parser raises a controlled
`FileNotFoundError` pointing at `docs/dle602-a3/semeval-restaurants.md`. Neither archive needs the
corpus, an accelerator or a network connection, because the trained artifacts are shipped and
inference runs on CPU.

Both archives are uploaded directly. The confirmed submission limit accepts the complete one, so
the lightweight archive is a faster download and never a size workaround, and no external link
appears anywhere in the submission. The report is uploaded as its own file and is deliberately not
bundled inside either archive, so the archive digests depend only on the source tree and the model
artifacts.

This file travels inside both archives, so it cannot state their digests without invalidating them.
They are published in the `v3.0.0` GitHub release and in the checksum note accompanying the upload.
`PACKAGE_MANIFEST.json` records the source commit and the size and digest of every entry one level
down.

## Known Limitations

- Core scope covers Restaurants only.
- Users must provide aspects; v3 does not extract them automatically.
- The SemEval benchmark is small, so model comparisons must retain the recorded overfitting and variance caveats.
- Attention or attribution does not establish a faithful causal explanation.
- Final performance and efficiency claims depend on a common verified run and must identify the recorded device.
- A clone or deployment that skips Git LFS receives pointer files in place of usable model artifacts.

## Issue Coverage and Final Gates

Completed foundation and evaluation: issues #73-#87, #91 and #94-#96. PR #100 merged the verified
supplemental six-model path into `main` at `0f02be3`. Independent validation landed afterwards:
Juan's Streamlit QA in PR #120 and PR #121, and Victor's reproduction and shipped-artifact
verification in PR #123 at `8787a73`.

Open release path:

1. [#88](https://github.com/lfariabr/review-pulse/issues/88) - confirm the group record and accept
   the evidence-backed report. The record and the report content are complete and frozen at
   `masters-swe-ai@6eaad14`; only the final PDF and DOCX export remains.
2. [#89](https://github.com/lfariabr/review-pulse/issues/89) - build both archives from the frozen
   commit with the final report inside, record their sizes and SHA-256 digests, extract and retest,
   then create the `v3.0.0` tag. The remote currently carries only `v3.0.0-rc.1`.

This file becomes the final release record when those gates are closed and the status is changed
from **release candidate** to **released**.
