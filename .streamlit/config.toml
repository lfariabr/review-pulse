[server]
# Transformers' vision models import torchvision at module inspection time,
# which Streamlit's default watchdog watcher triggers on hot-reload. Switching
# the watcher off avoids traversing transformers' lazy vision modules while the
# app is running. Restart Streamlit manually after code edits during development.
fileWatcherType = "none"
